terraform {
  backend "s3" {
    bucket         = "YOUR-BUCKET-NAME-ARSHIT"
    key            = "dev/terraform.tfstate"
    region         = "us-east-2"
    dynamodb_table = "terraform-lock-table"
    encrypt        = true
  }
}